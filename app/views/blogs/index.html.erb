<p id="notice"><%= notice %></p>

<h1>Blogs</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Date</th>
      <th>Topic</th>
      <th>Body</th>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
      <%= render @blogs %>
  </tbody>
</table>

<br>

<%= link_to 'New Blog', destroy_listing_path(listing) if logged_in?(:site_admin) %>